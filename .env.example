# Anthropic API Key (필수)
ANTHROPIC_API_KEY=sk-ant-...

# Database path (선택, 기본값: ./crawler/cve.db)
CVE_DB_PATH=./crawler/cve.db

# Web server port (선택, 기본값: 3000)
WEB_PORT=3000

# Crawler interval in hours (선택, 기본값: 2)
CRAWL_INTERVAL_HOURS=2
